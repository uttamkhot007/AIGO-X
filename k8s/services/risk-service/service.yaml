apiVersion: v1
kind: Service
metadata:
  name: risk-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: risk-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: risk-service
  ports:
    - name: http
      port: 8002
      targetPort: http
      protocol: TCP
