apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: compliance-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: compliance-service
  ports:
    - name: http
      port: 8003
      targetPort: http
      protocol: TCP
