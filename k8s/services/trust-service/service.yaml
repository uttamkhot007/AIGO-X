apiVersion: v1
kind: Service
metadata:
  name: trust-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: trust-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trust-service
  ports:
    - name: http
      port: 8009
      targetPort: http
      protocol: TCP
