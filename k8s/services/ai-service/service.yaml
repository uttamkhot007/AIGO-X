apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-service
  ports:
    - name: http
      port: 8008
      targetPort: http
      protocol: TCP
