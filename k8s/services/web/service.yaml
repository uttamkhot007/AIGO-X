apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
