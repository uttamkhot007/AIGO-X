apiVersion: v1
kind: Service
metadata:
  name: secops-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: secops-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: secops-service
  ports:
    - name: http
      port: 8007
      targetPort: http
      protocol: TCP
