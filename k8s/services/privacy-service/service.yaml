apiVersion: v1
kind: Service
metadata:
  name: privacy-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: privacy-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: privacy-service
  ports:
    - name: http
      port: 8005
      targetPort: http
      protocol: TCP
