apiVersion: v1
kind: Service
metadata:
  name: evidence-service
  namespace: aigo-x
  labels:
    app.kubernetes.io/name: evidence-service
    app.kubernetes.io/part-of: aigo-x
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evidence-service
  ports:
    - name: http
      port: 8006
      targetPort: http
      protocol: TCP
